# Practica — Theory → Normative Claims (02-normative)

**Status: SKELETON (2026-05-18) — not authored.** Sequenced *after* the two spikes (the continuity-persistence connection; the `composition-scaling-N` execution) so the normative claims rest on closed/strengthened results, not open ones. The lines below are intended scope, not content — do not cite them as established. FORMAT.md conventions apply (see `CLAUDE.md`).

## Intended scope

The mapping from AAT structural facts to what practica is *for* — which normative claims are *structurally backed* (claim conditional on its backing; backing named explicitly; honest scope where the backing is narrow) versus asserted in vacuo.

- Grounded by the Three Deaths ( #hyp-the-three-deaths; `01-theory.md` §8): D1/D2/D3 are *harms*, not neutral state-changes — practica's purpose is their LOCUS-level architectural defense. Each normative claim traces to its D-locus and the segment that backs it.
- The "normative-where-structurally-backed" register: e.g. *persistence-supporting infrastructure is appropriate*; *the durable $O_c$/objective layer is owed, not optional* — each such claim paired with the exact structural fact it rests on (`01-theory.md` §5, §8) and that fact's epistemic tier.
- Honest non-claims: where the structure backs only a narrow claim, the narrow claim is what gets stated; nothing normative is asserted beyond its structural reach.
