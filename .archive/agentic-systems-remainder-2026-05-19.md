# Agentic-systems remainder — hand-off list (verified at HEAD `9a3db8e`, 2026-05-19)

> **STATUS: FULLY DISCHARGED (agentic-systems HEAD `2e50bc2`, 2026-05-19).** Every item below was completed by the agentic-systems team (A1 + B6 landed into segments; A2/A3/A5 fixed; INDEX/ROUTING reconciled; C10 confirmed as a correctly-flagged source-side open, not a defect). Retained as a historical record of a verified hand-off — **not an open action list**. Provenance + the discharge are recorded in [[01-rewrite-plan]] rev 11.

*Written peer-to-peer for the agentic-systems agents: this is an inventory with verified status and caveats, not a work order — the segment-craft is yours. Every item is tagged with how I know it: **[V]** verified first-hand at HEAD `9a3db8e`; **[C]** contract/CHANGELOG self-report (read, not independently re-derived); **[U]** not verified — flagged for you to check. Provenance: surfaced during the practica `01-theory` source dive; A-1 / the 01-theory rewrite are practica-side and deliberately excluded here.*

**Correction to anything I said earlier:** an earlier statement that "the ε\*(N) re-typing is spike-only / canon is stale-pessimistic" is **false at HEAD** and withdrawn — both `#form-composition-closure` ε\*(N) rows are landed (below). Treat this file as superseding that.

---

## Settled — verified complete (no action; listed so you don't re-open)

- **[V] Continuity-persistence (𝒜_D, §03.II no-go) integration is complete per its own `INTEGRATION.md` contract.** `#der-turnover-information-recursion` is in `03-llm-core/OUTLINE.md`; `#disc-m-preservation` carries the affine-recursion result with the "multiplicative loss" correction (omission-fix per CHANGELOG, adjudged 𝒜_D-faithful); `#result-sector-persistence-template` carries the two-model typed-bridge subsection + row 7; the `04-eli-core` motivational cross-ref exists verbatim at `#hyp-the-three-deaths` line 78 (constraint 4 satisfied).
- **[V] T3 / (E-A) barrier-reachability + (M-ADD)/(M-FREE) are correctly carried as named open-edges** in `#der-identity-continuity-threshold` Working Notes / (M-FREE) / Epistemic Status — which is what `INTEGRATION.md` specifies (open edges → Working-Notes scope, `#hyp-substrate-transfer-asymmetry` style). The 99-verdict §6 "→ separate hyp-/disc- segment" was superseded by that contract. Done as intended, not undone.
- **[V] ε\*(N) status rows landed.** `#form-composition-closure` line 177 (the "$N$-agent scaling" row) now states *"No exponential regime exists"*; line 180 (the "Strategy-DAG projection under $\Lambda_x$" row) is *"Resolved (conditional; one named sub-open Q1)"*. The accumulation-typing convention (D1) is landed in `NOTATION.md`.

## A — Integration loose-ends (resolved in substance, not fully in canon — the actionable debt)

1. **[V] The strategy-DAG-composition derivation is row-summary-only; the math is still spike-resident.** A grep for the actual results (`SCC-condensation` / `order-incompat` / `τ-abstraction` / `S3a` / total-correlation defect) across `01-aat-core/src 03-llm-core/src 04-eli-core/src` returns **only** `form-composition-closure.md` — i.e. the one-row summary + the `spike-strategy-dag-composition.md`/CHANGELOG citation. The S3a closed form, the SCC-condensation/TC-defect law, and the no-go (no exact single-DAG macro-strategy) are not in a segment body. Per *math-lives-in-segments* this is the parent's signature failure mode. Judgment, not prescribed: whether a "Resolved (conditional)" row citing a spike+CHANGELOG suffices, or the derivation must land in `#form-composition-closure` / a new segment, is yours + Joseph's call — but it is the single largest real gap.
2. **[V] Internal canon contradiction: `#der-tempo-composition` line 98 still says *"Open: does ε\* scale with N? … linear or superlinear"*** while `#form-composition-closure` line 177 now says *"No exponential regime exists."* Two segments disagree at HEAD. This is worse than staleness — it's a live contradiction a reader will hit. Needs the N-free answer propagated to `der-tempo-composition` (and its Brooks's-Law "superlinear?" framing reconciled).
3. **[V] `#der-team-persistence` Working Notes line 109 is a stale continuity breadcrumb** (added 2026-05-18 "while grounding ~/src/practica" — this dive caused it; 01-theory §12 cites it). It still says the dynamic across-turnover theorem is *"still discussion-grade — the (scaffolded, not de-novo) spike target."* False now: resolved into `#der-turnover-information-recursion` / `#der-identity-continuity-threshold`. Should point at the resolved segments.
4. **[V] `spikes/INDEX.md` is stale** — HEAD commit message is literally *"Archive integrated spikes … INDEX WASN'T UPDATED."* The continuity-persistence row (line 24) still says "integration pending review" though it's canon-wired. Cosmetic but it mis-signals state to the next agent (the failure mode the routing discipline exists to prevent).
5. **[C] The `CHANGELOG.md` 2026-05-19 "What is NOT done — gated on Joseph" subsection is itself stale.** It lists as still-gated: the shared-upstream `#result-sector-persistence-template` restatement and the `#form-composition-closure` Open-row re-typing (D2). Both have **since landed** (verified [V] above). The CHANGELOG entry says "uncommitted at time of writing"; subsequent commits closed them but the "NOT done" prose was not updated. Documentation-integrity fix so the CHANGELOG doesn't mislead.

## B — Open decisions gated on Joseph (named, not defects)

6. **[C] P2 (uniqueness of the compensation channel, frozen-substrate) landed *folded* into `#der-identity-continuity-threshold`'s Discussion, not as the standalone `disc-`/`#def-identity-sufficiency`-Discussion segment 99-verdict §6 recommended.** Substance is in canon; the placement deviates from the recommendation. Decision: accept the fold, or extract a standalone P2 segment (the two-channel scoping — fast/episodic relational vs slow/coarse weight-consolidation — is citable on its own and connects to `#hyp-substrate-transfer-asymmetry`).
7. **[C] The D3 register question is genuinely open and deliberately un-landed** (CHANGELOG 2026-05-19): whether the accumulation-typing / certificate-spine relationship is an independent fifth meta-segment or the representation-dual of `#disc-stability-certificate`. No discussion segment was created (creating one pre-empts D3). Joseph's call; `msc/for-joseph-acc-confound.md` is the live decision aid.

## C — Genuine open theory (legitimately undone, named first-class — frontier, not debt)

8. **[V] Continuity second-no-go (practica-load-bearing).** `#der-turnover-information-recursion` §4.2 / Working Notes: adversarially-correlated reinjection that vanishes exactly when $I_k$ is low — flagged "the single most valuable follow-on." CHANGELOG line 41 notes the "fight"/resource-budget regenerative regime is a structural cousin of this same open second-no-go.
9. **[V] `#der-identity-continuity-threshold` promotion path:** barrier-reachability/QSD (guarantee vs merely postpone), (M-ADD) non-additive generalization, state-dependent $\varrho_{\text{rg}},\rho$ coupling. The genuinely open theorem on the identity side.
10. **[V] `RECONCILIATION.md` §4 open question (could demote a segment):** whether `#der-turnover-information-recursion`'s R1/R2 is genuinely novel vs. a bridge-lemma-resolvent instance of the existing 𝒜_D family — explicitly "test-not-assert; segment demotable to a worked instance if it resolves 'subsumed.'" Worth listing because its resolution changes a segment's standing.
11. **[V] Smaller named edges:** ε\*(N) Source-3 Q1 (SCC-defect constant tightness for non-deterministic intra-SCC coupling); the SDPI coefficient $\eta_k$ uncomputed for any concrete `f_init` (e.g. practica's durable layer); `#der-tempo-composition` remains `status: sketch` (precise $C_{\text{coord}}$ uncertified — 01 §3 already tiers this faithfully, so no fidelity issue, just unfinished theory).

## D — What I did NOT verify (so this list does not imply completeness)

- **[U]** I did not independently re-derive any spike math (strategy-DAG-composition §3/§6, the continuity SDPI argument). Items tagged [C] rest on the spikes'/contract's own statements read at the framing/verdict level.
- **[U]** I did not audit `#der-identity-continuity-threshold` against `INTEGRATION.md` constraint 4 ("no Volume IV identity theorem; $S_{\text{id}}$ is one normalization, not the object"). The `result-sector-persistence-template` two-model subsection + `RECONCILIATION.md` assert the 𝒜_D/𝒜_refl split is clean and the identity object legitimately distinct; I read those assertions, I did not re-audit the segment against the constraint. Given the cross-volume mis-frame was this spike's original sin, an independent check that `der-identity-continuity-threshold` doesn't re-introduce it is worth doing before the continuity work is declared fully closed.
- **[U]** CHANGELOG commit hashes (`b841e40`, `3054dbc`, "this cycle's … uncommitted at time of writing") vs. working tree state — I did not reconcile what is committed vs. dirty; if a landing is uncommitted, item-A status could shift.
